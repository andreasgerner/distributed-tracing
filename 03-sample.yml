apiVersion: apps/v1
kind: Deployment
metadata:
  name: company-deployment
  labels:
    app: company
spec:
  replicas: 1
  selector:
    matchLabels:
      app: company
  template:
    metadata:
      labels:
        app: company
      annotations:
        "resource.opentelemetry.io/service.name": "company-service"
        "instrumentation.opentelemetry.io/inject-java": "true"
    spec:
      containers:
        - name: company
          image: ghcr.io/andreasgerner/distributed-tracing/sample-company:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: PAYMENT_URL
              value: "http://payment-service:8080"
            - name: DB_URL
              value: jdbc:sqlserver://host.docker.internal:10001;encrypt=true;trustServerCertificate=true;username=sa;password=yourStrongPassword1!;
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: company-service
  labels:
    app: company
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: company
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: company-ingress
spec:
  rules:
    - host: company.localhost
      http:
        paths:
          - backend:
              service:
                name: company-service
                port:
                  name: http
            pathType: ImplementationSpecific
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-deployment
  labels:
    app: payment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
      annotations:
        "resource.opentelemetry.io/service.name": "payment-service"
        "instrumentation.opentelemetry.io/inject-java": "true"
    spec:
      containers:
        - name: payment
          image: ghcr.io/andreasgerner/distributed-tracing/sample-payment:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: DB_URL
              value: jdbc:sqlserver://host.docker.internal:10002;encrypt=true;trustServerCertificate=true;username=sa;password=yourStrongPassword1!;
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  labels:
    app: payment
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: payment
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payment-ingress
spec:
  rules:
    - host: payment.localhost
      http:
        paths:
          - backend:
              service:
                name: payment-service
                port:
                  name: http
            pathType: ImplementationSpecific
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-angular-deployment
  labels:
    app: web-angular
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-angular
  template:
    metadata:
      labels:
        app: web-angular
    spec:
      containers:
        - name: web-angular
          image: ghcr.io/andreasgerner/distributed-tracing/sample-web-angular:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: web-angular-service
  labels:
    app: web-angular
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: web-angular
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-angular-ingress
spec:
  rules:
    - host: web-angular.localhost
      http:
        paths:
          - backend:
              service:
                name: web-angular-service
                port:
                  name: http
            pathType: ImplementationSpecific
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-next-deployment
  labels:
    app: web-next
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-next
  template:
    metadata:
      labels:
        app: web-next
      annotations:
        "resource.opentelemetry.io/service.name": "web-next"
        "instrumentation.opentelemetry.io/inject-nodejs": "true"
    spec:
      containers:
        - name: web-next
          image: ghcr.io/andreasgerner/distributed-tracing/sample-web-next:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: COMPANY_URL
              value: "http://company-service:8080"
            - name: PAYMENT_URL
              value: "http://payment-service:8080"
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: web-next-service
  labels:
    app: web-next
spec:
  ports:
    - name: http
      port: 80
      targetPort: 3000
  selector:
    app: web-next
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-next-ingress
spec:
  rules:
    - host: web-next.localhost
      http:
        paths:
          - backend:
              service:
                name: web-next-service
                port:
                  name: http
            pathType: ImplementationSpecific
